// S0.6 spike module — exists only so `go run ./go-validator` works from this
// directory inside the golang:1.24-alpine container. Pure stdlib, no deps,
// no go.sum. Throwaway per the CLAUDE.md spike carve-out.
module kwetu/spikes/s06

go 1.24
