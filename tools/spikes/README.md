Throwaway Phase-0 spike probes per the CLAUDE.md carve-out — never production code, never imported by `src/`.
Each spike folder (`S0.x-*`) is self-contained and may be deleted once its ADR lands in `docs/adr/`.
